self.addEventListener("push",event=>{const data=event.data?event.data.json():{};event.waitUntil((async()=>{const clientList=await clients.matchAll({type:"window",includeUncontrolled:true});for(const c of clientList)c.postMessage(data);const anyFocused=clientList.some(c=>c.focused);if(data.type==="balance_update"&&anyFocused)return;await self.registration.showNotification(data.title||"Wave Admin",{body:data.body||"New admin event",icon:data.icon||"/vite.svg",badge:data.badge||"/vite.svg",data:{url:data.url||"/"}});})());});
self.addEventListener("notificationclick",event=>{event.notification.close();const url=event.notification.data?.url||"/";event.waitUntil(clients.matchAll({type:"window",includeUncontrolled:true}).then(list=>{const open=list.find(c=>c.url.includes(location.origin));return open?open.focus():clients.openWindow(url);}));});
